<!DOCTYPE html>
<html lang="pt-br">

<head>
    <title>Detalhe | Desapegados</title>
    <link rel="stylesheet" href="/stylesheets/pagDetaProduto.css">
    <link rel="shortcut icon" href="/images/home/logo-D.ico" type="x-icon">
</head>

<body>
    <header>
        <%- include('./partials/header') %>
    </header>

    <div class="conteudo">
        <div>
            <div>
                <p class="nome-produto"><%= produtoid.nome %></p>
            </div>
        </div>
        <div class="caixa-produto">
            <div>
                <div class="caixa-imagem">
                    <img class="img" src="/images/produtos/<%= produtoid.src %>" alt="Imagem do Produto">
                    <p>
                        <%= produtoid.descricao %>
                    </p>
                </div>
            </div>
            <div class="caixa-preco-opcoes">
                <article>
                    <h2><%= produtoid.name %></h2>
                <%
                    let precoFinal=(produtoid.preco * (1 - produtoid.desconto)).toFixed(2).replace('.',',')
                    let preco = (produtoid.preco).toFixed(2).replace('.',',')
                    if(produtoid.desconto> 0) { %>
                        <p>
                            <span class="preco-antes">R$ <%= paraMil(preco) %></span> /
                            <span class="porcentagem-desc"><%= produtoid.desconto *100 %>% OFF</span>
                        </p>
                        <p class="preco-depois">R$ <%= paraMil(precoFinal) %></p>
                    <% } else { %>
                        <p class="preco-depois">R$ <%= paraMil(precoFinal) %></p>
                    <% } %>
                <div class="vantagens">
                    <p><img class="icone" src="/images/pagDetaProduto/credit-card.png" alt="Cartão de Crédito">Pague em até 12 parcelas sem juros</p>
                    <p><img class="icone" src="/images/pagDetaProduto/delivery-van.png" alt="Carro de Entrega">Retire gratuitamente nas instalações do vendedor</p>
                </div>
                    <a class="botao-comprar" href="">COMPRAR AGORA</a>
                    <hr>
                       <form action="/products/delete/<%= produtoid.id %>?_method=DELETE" method="POST">
                        <a class="botao-editar" href="/products/edit/<%= produtoid.id %>">EDITAR PRODUTO</a>
                        <button class="botao-apagar" type="submit" class="action-button delete">APAGAR</button>
                    </form>
                    </article>
                </div>
            </div>
        </div>
    </div>

</body>
<footer>
    <%- include('./partials/footer') %>
</footer>

</html>